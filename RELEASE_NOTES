A new version of My Tester is out. Parameter $config of ContainerFactory::create()/TCompiledContainer::refreshContainer() now works correctly.

For complete list of changes since previous version, see CHANGELOG.md.
