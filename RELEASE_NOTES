A new version of My Tester is out. Data set is now shown for skipped jobs or jobs passed with warnings, just like for failed jobs. It also contains a few internal improvements.

For complete list of changes since previous version, see CHANGELOG.md.
