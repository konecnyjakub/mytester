A new version of My Tester is out. It adds assertions assertGreaterThan and assertLessThan. Tests can be marked as incomplete with method TestCase::markTestIncomplete().

For complete list of changes since previous version, see CHANGELOG.md.
