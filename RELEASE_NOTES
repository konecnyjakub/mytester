A new version of My Tester is out. Parameter $config of ContainerFactory::create()/TCompiledContainer::refreshContainer() now works correctly. Test methods are no longer skipped if their data provider did not provide optional parameters.

For complete list of changes since previous version, see CHANGELOG.md.
