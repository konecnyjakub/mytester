language: php

php:
    - 5.6
    - 7.0
    - 7.1
    - 7.2
    - nightly

notifications:
    email: false

env:
    - PHP_BIN=php
    - PHP_BIN=php-cgi

matrix:
    allow_failures:
        - php: 7.2
        - php: nightly

before_install:
    - composer self-update

install:
    - wget https://www.phing.info/get/phing-latest.phar && mv phing-latest.phar phing && chmod +x phing
  
script:
    - ./phing -Dbinary=$PHP_BIN

cache:
    directories:
        - $HOME/.composer/cache
        - vendor
